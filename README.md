# typescript-graphql-exp
In this project, we set up a simple GraphQL server using Apollo Server.
Prerequisites: Ability to build a full-stack application with Node.js and a front-end library/framework like React, some familiarity with both GraphQL and TypeScript.
